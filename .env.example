# PHP API base (no trailing slash). Storefront auth uses /auth/* on this same origin (session cookies).
# Run backend: cd ../ecom && php -S localhost:8000 -t public proxy.php
REACT_APP_API_URL=http://localhost:8000/api/v1.0.0
